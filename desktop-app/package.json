{
	"name": "desktop-app",
	"productName": "Electron",
	"private": true,
	"version": "1.0.0",
	"description": "Desktop version of The More The Merrier",
	"license": "MIT",
	"main": "dist-electron/main/index.js",
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build && electron-builder"
	},
	"engines": {
		"node": "^14.18.0 || >=16.0.0"
	},
	"debug": {
		"env": {
			"VITE_DEV_SERVER_URL": "http://127.0.0.1:7777"
		}
	},
	"devDependencies": {
		"electron": "^21.1.0"
	},
	"dependencies": {
		"the-more-the-merrier-codebase": "file:.."
	}
}
